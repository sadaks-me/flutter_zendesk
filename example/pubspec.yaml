name: flutter_zendesk_example
description: Demonstrates how to use the flutter_zendesk plugin.
publish_to: 'https://pub.dev/packages'
version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_zendesk:
    path: ../

flutter:

  uses-material-design: true